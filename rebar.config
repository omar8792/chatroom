{erl_opts, [debug_info]}.
{deps, []}.

{relx, [{release, {chatroom, {git, long}},
         [chatroom]},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true},

        {overlay, []}]}.

{shell, [
    {apps, [chatroom]}
]}.
